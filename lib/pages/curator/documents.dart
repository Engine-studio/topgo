import 'package:flutter/widgets.dart';

class CuratorAndAdminDocumentsTab extends StatelessWidget {
  const CuratorAndAdminDocumentsTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
